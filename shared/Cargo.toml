[package]
name = "rust-fullstack-template-shared"
version = "0.1.0"
authors = ["{{authors}}"]
edition = "2018"

[dependencies]
bumpalo = { version = "3.6.1", features = ["collections"] }
lignin = "0.0.5"
lignin-schema = "0.0.4"
